# AGENTS.md

Agent-facing operational notes for this repo. Product/feature docs live in `AI_Personal_Relationship_Assistant_Docs/`; this file is about *how to work in the repo*, not what to build.

## What this is
Laravel 13 API backend for an AI-powered WhatsApp relationship assistant (see `AI_Personal_Relationship_Assistant_Docs/PRD.md`). Scaffolded by Antigravity, continued in Claude Code.

## Environment gotchas
- **PHP version**: this repo requires PHP **>=8.4.1** (Laravel 13 + composer.lock). XAMPP's bundled PHP on this machine is only 8.2.12 and cannot run `artisan`/composer here. Use Laravel Herd's PHP instead:
  `"/c/Users/nugro.FAJAR-NB/.config/herd/bin/php.bat" artisan ...` (Herd has PHP 8.4.22 installed).
- **Database**: MySQL via XAMPP/MariaDB, not SQLite (migrated away from the SQLite starter). Database name `jarwo`, host `127.0.0.1:3306`, user `root`, no password. `database/database.sqlite` was deleted — don't recreate it. (`phpunit.xml` still uses `DB_CONNECTION=sqlite` + `DB_DATABASE=:memory:` for the test suite only — that's intentional Laravel default, leave it.)
- **AI provider**: abstracted behind `AIProviderInterface` (`app/Services/AI/`), selected via `AI_DRIVER` env. Default is **Gemini** (`gemini-2.5-flash`); OpenAI is the alternate implementation. Real API key is set in `.env` (gitignored).
- **WhatsApp gateway**: abstracted behind `WhatsAppGatewayInterface` (`app/Services/WhatsApp/`), selected via `WHATSAPP_DRIVER` env. Default is **Fonnte**; Wablas is the alternate. Meta Cloud API is not implemented.
- **Inbound webhook is single-tenant**: `WHATSAPP_OWNER_EMAIL` in `.env` names the one app `User` that all Fonnte-webhook contacts/messages get attributed to (there's no per-user WhatsApp device mapping — matches the rest of the app's global-env-driven config). Must point at a real, already-registered user or the webhook 500s.

## Architecture pointers
- `App\Services\Memory\MemoryEngine` is the core business logic (remember/recall/forget/analyzeAndStore/buildOrUpdateStyleProfile/suggestReply) — controllers should call into it rather than touching `Memory`/`StyleProfile` models directly.
- Both provider interfaces are wired as singletons in `App\Providers\AppServiceProvider::register()`. Add new drivers there.
- API routes live under `/api`, Sanctum-protected except auth register/login and `POST /webhooks/fonnte/{secret}` (verified via shared-secret path segment, not Sanctum — Fonnte can't send a bearer token). Contact-scoped resources (`messages`, `suggest-reply`, `memories/analyze`, `style-profile`) are nested under `/contacts/{contact}/...`, not top-level.
- `GET /contacts/{contact}/style-profile` (added 2026-07-17, `MemoryController::styleProfile`) is a passive read of the `StyleProfile` row — `{"style_profile": null}` if none exists yet. Exists specifically so a client can *display* the current style profile without triggering `POST /contacts/{contact}/memories/analyze`'s AI call every time (that endpoint always re-runs analysis, it's not a getter). Added for the `jarwo_app` mobile client's style-profile screen — see the "Companion mobile app" section below.
- **Unauthenticated requests without `Accept: application/json` used to 500, not 401.** This app has no `login` web route (API-only, no session/Blade auth flow), but Laravel's default guest-redirect behavior eagerly calls `route('login')` to build a redirect target whenever it thinks a request wants a redirect rather than JSON — and that call itself throws `RouteNotFoundException` before any exception handler gets a chance to render a clean response. Confirmed in production 2026-07-17 from a plain `curl -F ...` request with no `Accept` header. Fixed in `bootstrap/app.php`: `$middleware->redirectGuestsTo(fn () => null)` (never redirect) paired with `$exceptions->render(AuthenticationException::class, ...)` returning JSON 401. Both pieces are required — the render() callback alone doesn't help since the crash happens before `AuthenticationException` is even constructed. Don't remove either half.
- `App\Http\Controllers\Api\WebhookController::fonnte` is the inbound message entry point: creates/finds the Contact, stores the Message (deduped by `inboxid`), and — if auto-reply is enabled (see below) — dispatches `App\Jobs\ProcessAutoReply` with a delay. The controller itself never calls the AI or sends anything synchronously anymore; all of that lives in the job.
- **Fonnte's `inboxid` is not a reliable unique id**: a real inbound message tested 2026-07-16 came through with `inboxid: "0"` (not a genuine per-message id, despite what the docs imply). The webhook treats `inboxid` values of `0`/empty as "no id provided" and skips the dedup check in that case, rather than risk silently dropping distinct messages that all report `0`. Don't "simplify" this back to a plain truthy check.
- **Auto-reply is gated two-tier and opt-in**: `$contact->ai_enabled` (per-contact, `contacts` table, default **`false`** since 2026-07-17) AND `$user->getSetting('auto_reply_enabled', false)` (global) both have to be true, checked both at dispatch time (`WebhookController::fonnte`) and again at job execution time (`ProcessAutoReply::handle`, since state can change during the debounce delay). Pattern copied from the sibling `c:\xampp\htdocs\chatbot-crm` project (Laravel + Livewire chatbot CRM, uses a global `Setting` key `ai_paused` plus a per-conversation `ai_enabled` column the same way) — the opt-in default is this project's own addition, deliberately backwards from `chatbot-crm`'s opt-out default, because the owner wants to pick which contacts get auto-replied to before it happens, not disable it after. Flipped via migration `2026_07_17_071943_change_ai_enabled_default_to_false_on_contacts` (changes the column default AND resets all existing rows to `false` — a genuine data reset, not just a schema change). `WebhookController::fonnte` explicitly passes `'ai_enabled' => false` when creating new contacts (see the `firstOrCreate` hydration gotcha below for why this must be explicit, not left to the DB default).
- **Fonnte's send API returns `id` as an array**, e.g. `{"id": ["abc123"], "status": true}`, not a scalar. `FonnteGateway::sendMessage()` (and `WablasGateway::sendMessage()`, defensively) flattens `id` to its first element before returning, so callers can store it directly in the `gateway_message_id` column. Found via a real crash (`ErrorException: Array to string conversion`) during live auto-reply testing on 2026-07-16 — **the crash happened after the real Fonnte send API call had already gone out**, so the AI reply was very likely actually delivered to the contact even though the app failed to record it. If you ever see this exception again, assume a real message went out with no DB record of it.
- **`Contact::firstOrCreate()`'s in-memory instance doesn't have DB-default columns hydrated.** Confirmed via tinker: right after creating a brand-new contact without explicitly passing `ai_enabled`, `$contact->ai_enabled` is PHP `null` in that same request even though the DB row gets the column's real default (MySQL applies its own column default, which Eloquent doesn't fetch back without a `fresh()`/`refresh()`). This silently broke auto-reply for a contact's very first message (`null && anything` = false in the gate check) — fixed by having `WebhookController::fonnte` always pass `'ai_enabled'` explicitly in the `firstOrCreate` values array rather than relying on the DB default. Any other place that creates a `Contact` (or any model with DB-default columns) and immediately branches on one of those columns needs the same treatment.
- **WhatsApp chat export import** (`App\Services\WhatsApp\WhatsAppExportParser`, `MessageController::import`, `POST /contacts/{id}/messages/import`) exists to solve a real gap: the owner's own WhatsApp replies (typed from their own phone) never reach this app through any other path — Fonnte's webhook only forwards *inbound* messages, so without this, the style profile/memory system only ever had AI-generated auto-reply text to learn from (learning from itself, not the owner). Parser handles both WhatsApp export line formats (`[d/m/y, H:i:s] Name: text` and `d/n/y, H:i - Name: text`, dash may be `-` or `–`), non-zero-padded dates (`1/1/26`, not `01/01/26` — uses Carbon's `j`/`n` format chars, not `d`/`m`), multi-line messages (continuation lines with no timestamp prefix get appended to the previous message), and skips media placeholders/the standard encryption notice. `owner_name` must be passed as an exact string match to how the owner's own name appears as sender in that specific export (e.g. `"Fajar Dwi Nugroho"`, not a nickname) — everything else is treated as the contact's messages. No dedup against already-existing messages — re-importing the same file twice will duplicate rows; not handled yet, acceptable for now since this is a rare, deliberate one-off action per contact.
- **Real WhatsApp exports contain invalid UTF-8 byte sequences** (confirmed 2026-07-17 importing a real years-long export: MySQL error 1366 `Incorrect string value`, e.g. a lone `0xC2` lead byte with no valid continuation — likely from a lossy encoding step somewhere in how the phone/WhatsApp produced the export). This is genuinely malformed UTF-8 (not the classic "utf8 3-byte column can't hold a 4-byte emoji" issue — a lone incomplete lead byte is invalid in *any* UTF-8 width, utf8mb4 included; `config/database.php`'s `charset`/`collation` already default to `utf8mb4`/`utf8mb4_unicode_ci` for every environment via Laravel's own stock config, so this was never a charset-mismatch problem, don't waste time re-checking table charset for this specific error). Two separate fixes were needed, both required: (1) `WhatsAppExportParser::sanitize()` runs on each **line before** regex matching, not just on the matched content after — the patterns use PCRE's `/u` modifier, which makes `preg_match` fail to match *at all* (not partially) when the subject has invalid UTF-8, silently dropping the whole line if sanitizing only happened post-match. Uses `iconv('UTF-8', 'UTF-8//IGNORE', ...)` to drop bad bytes (more reliable across environments than `mb_convert_encoding`, whose invalid-byte handling depends on the `mbstring.substitute_character` ini setting). (2) `MessageController::insertResiliently()` bulk-inserts in chunks of 200 and falls back to one-row-at-a-time within any chunk that still fails after sanitizing — a single bad row shouldn't take down an entire multi-thousand-message import.
- **Import AI analysis moved off the request** (2026-07-17): `MessageController::import()` originally called `analyzeAndStore`/`buildOrUpdateStyleProfile` inline right after saving the imported messages. For a real multi-year chat export, that's two sequential AI calls after a large bulk insert, all inside one HTTP request — risks exceeding the host's request timeout with **no response returned at all** (worse than a clean error). Fixed by dispatching `App\Jobs\AnalyzeContactHistory` (queued, picked up by the same cron-driven `schedule:run` mechanism as `ProcessAutoReply`) instead of calling the AI inline. The import response now returns as soon as messages are saved, with `"analysis": "queued"` — verified locally that `import()` itself makes zero AI HTTP calls (`Http::assertNothingSent()`), and that the dispatched job correctly produces the `Memory`/`StyleProfile` rows once `schedule:run` processes it.
- **Default persona + periodic auto-reanalysis** (2026-07-17): two related additions so a brand-new contact doesn't get a permanently generic AI voice. (1) `App\Jobs\AnalyzeImportedHistory` was renamed to **`App\Jobs\AnalyzeContactHistory`** since it's no longer import-only — it's now dispatched from two places: `MessageController::import()` (unchanged) and `WebhookController::fonnte()` (new). (2) `WebhookController::fonnte()` now counts messages for a contact created since `optional($contact->styleProfile)->last_analyzed_at ?? $contact->created_at`, and dispatches `AnalyzeContactHistory` once that count reaches `whatsapp.auto_reanalyze_message_interval` (env `AUTO_REANALYZE_MESSAGE_INTERVAL`, default 15, set to 0 to disable) — so the style profile keeps improving from real conversation without the owner remembering to tap "Analisa Ulang" manually. This check is deliberately independent of `ai_enabled`: style learning should happen from any conversation (including the owner's own replies sent through `jarwo_app`), not just auto-reply traffic, since the owner may want a contact's style well-understood *before* ever turning auto-reply on for them. (3) Until a contact has its own `StyleProfile`, `MemoryEngine::resolveStyleData()` (new private helper, used by both `suggestReply` and `suggestAutoReply`, replacing duplicated inline lookups) falls back to the user's global `default_persona` `Setting` (plain text, no new endpoint — just another key through the existing `/settings` CRUD) as `['summary' => ...]`, so a first-contact reply isn't completely generic while waiting for real history to accumulate. A real per-contact `StyleProfile`, once it exists, always takes priority over the default persona. Verified via tinker: fallback applies with no profile, real profile overrides it once present, and the message-count threshold trips at exactly the configured interval (14 messages → no dispatch, 15th → dispatch). `jarwo_app`'s Settings screen exposes a text field for editing `default_persona` (reuses the existing generic settings PUT, no new endpoint needed there either).
- **Fonnte marks every inbound WhatsApp message as read on the phone by default, regardless of `ai_enabled`** (found 2026-07-17 via real device testing through `jarwo_app`). This is NOT our code — confirmed by reading `WebhookController::fonnte` line by line, it never calls anything read-status-related, it only stores the inbound `Message` row. It's Fonnte's own device-level **"autoread"** toggle (Fonnte dashboard → Device settings), which is on by default and needed for the chatbot/webhook flow to work at all — turning it off is not the fix. The actual fix is Fonnte's separate **"Silent Read"** toggle ("read but prevent fonnte from mark it as read") — enabling it keeps webhook delivery working exactly the same while no longer sending a WhatsApp read receipt, so contacts with `ai_enabled = false` correctly stay "unread" on the owner's phone instead of silently getting marked read and missed. Purely a Fonnte dashboard setting, no code change. If this ever resurfaces as "messages disappear/get missed even with autoreply off," check this toggle before assuming it's an app bug.
- **Contact's manual `notes` field now feeds AI replies** (2026-07-18): previously `notes` (a free-text field the owner can write per contact, e.g. "adik ipar, suka bercanda") was stored/displayed but never read by anything AI-related — pure decoration. `MemoryEngine::resolveMemories()` (new private helper, used by both `suggestReply` and `suggestAutoReply` in place of a bare `recall()` call) now prepends `notes` as a labeled synthetic memory ("Catatan pemilik akun tentang kontak ini: ...") ahead of any stored `Memory` rows, so it flows through the exact same `ReplyGenerationPrompt` memory-list rendering with no interface/provider changes needed. Verified via tinker: notes-only contact surfaces exactly one entry, a contact with both notes and a real stored memory gets both (notes first), and a contact with no notes gets an empty list (nothing injected).

## Known bug pattern — don't reintroduce
`memories`, `events`, `messages`, and `style_profiles` each have a foreign key straight to `users.id` **and** an indirect path through `contacts.user_id`. Deleting a `User` in one DB-level cascading statement hits MySQL/InnoDB's cascade-diamond limitation (error 1452) — confirmed by testing a raw `DELETE FROM users`. Fixed by explicit deletion order in `App\Models\User::booted()` (deletes tokens/messages/memories/events/styleProfiles/settings/contacts before the user row goes). Do not remove that hook or assume DB-level `ON DELETE CASCADE` alone handles user deletion.

**`memories.contact_id` must be `cascadeOnDelete()`, not `nullOnDelete()`** (bug found + fixed 2026-07-18, migration `2026_07_17_232753_change_memories_contact_id_to_cascade_on_delete`). It was originally created `nullOnDelete()` — deleting a Contact left its Memory rows behind with `contact_id` set to `NULL` instead of removing them. `MemoryEngine::recall()` treats `contact_id IS NULL` memories as *global*, applicable to every contact (`orWhereNull('contact_id')`, by original design for genuinely global facts created via `POST /memories` with no `contact_id`) — so any memory orphaned this way silently became global context injected into **every future AI reply for every other contact, permanently**, not just a leftover row. Confirmed via tinker: deleting a contact with an attached memory now actually deletes the memory (`Memory::find($id)` returns null afterward) instead of nulling it. `messages.contact_id` and `style_profiles.contact_id` were already correctly `cascadeOnDelete()` — only `memories` had this bug. `events.contact_id` is intentionally left `nullOnDelete()` (an event not tied to any contact is a normal, valid state — events aren't fed into AI reply context like memories are, so there's no equivalent leak risk).

## Exposing the local server for real webhook testing
`cloudflared` is installed on this machine (`C:\Program Files (x86)\cloudflared\cloudflared.exe`). To let Fonnte reach the local webhook for a real end-to-end test:
1. `php artisan serve --port=8123` (use the Herd PHP, see above)
2. `cloudflared tunnel --url http://localhost:8123` — prints a random `https://<words>.trycloudflare.com` URL after a few seconds (in the log, look for the boxed "Your quick Tunnel has been created!" line)
3. Configure that URL + `/api/webhooks/fonnte/{secret}` in the Fonnte device dashboard's webhook field

This is a genuine public tunnel to local `.env` secrets and the live DB — **always confirm with the user before starting one** (Claude Code's auto-mode classifier blocks it without explicit confirmation anyway), and tear both the tunnel and `artisan serve` process down again once done testing.

**Quick tunnels silently die.** Observed 2026-07-16: a tunnel left running ~9 hours (registered in Fonnte's dashboard, idle in between real-world tests) went into an infinite `control stream encountered a failure while serving` / `Retrying connection` loop and never recovered, while the `cloudflared` process kept running and looked healthy from `Get-Process`. Symptom from the outside: Fonnte "goes quiet" — inbound messages just stop arriving, no error anywhere in Laravel's log because the request never reaches it. Before trusting a "no messages arrived" result during a longer test session, re-verify the tunnel first: `curl -o /dev/null -w "%{http_code}" <tunnel-url>/api/webhooks/fonnte/wrongsecret -X POST` should return `403` (proves the request is actually reaching Laravel); anything else (timeout, 530, connection error) means the tunnel is dead and needs restarting — which mints a *new* random URL that has to be re-registered in the Fonnte dashboard.

## Auto-reply pipeline: debounce, needs_reply, daily ceiling
Built in response to two real problems hit during live testing on 2026-07-16: (1) a contact ("Jus Sehat Kopi Nikmat") that has its own auto-responder produced 8 inbound messages in ~90 seconds — consistent with a bot-vs-bot reply loop with no human involved; (2) a person splitting one thought across several WhatsApp bubbles (e.g. "assalamualaikum" / "bro nanti malam aku main kerumahmu" / "aman ta?") would get an out-of-context reply to the first fragment alone.

**Design, and why simpler alternatives were rejected** (don't re-propose these without re-reading this section):
- ~~Keyword-based "closing phrase" detection~~ — rejected, can't scale to regional language/slang ("suwun", "syukron", "aman bro").
- ~~Rate-limit / velocity cap (e.g. "max 5 replies per 2 minutes")~~ — rejected, proven unreliable: a real fast human exchange (short back-and-forth planning chat) can trip the same velocity a bot loop would, and there's no way to tell them apart from timing alone.
- **What's actually implemented**, in `App\Jobs\ProcessAutoReply` (dispatched with a delay by `WebhookController::fonnte`, not called synchronously):
  1. **Debounce** (`AUTO_REPLY_DEBOUNCE_SECONDS`, default 12s): the job is dispatched with `->delay()`. When it runs, it checks whether a *newer* inbound message from the same contact has arrived since — if so, it bails silently (the job dispatched for that newer message supersedes it and will see the full, combined conversation). Only the job for the last message in a burst actually fires.
  2. **Semantic `needs_reply` judgment**: `AIProviderInterface::generateAutoReply()` (new interface method, both providers implement it) asks the AI to read the recent conversation and decide, by meaning rather than fixed keywords, whether the exchange has reached a natural stopping point — in any language/dialect/slang — before generating a reply. Returns `{"needs_reply": bool, "reply": string}` in one call (see `ReplyGenerationPrompt::systemForAutoReply()`).
  3. **Daily ceiling** (`AUTO_REPLY_DAILY_LIMIT`, default 25) — not a loop detector, a blunt cost/spam backstop. Counts today's outbound auto-replies per contact; if hit, auto-disables `contact->ai_enabled` and logs a warning rather than silently doing nothing. Deliberately generous so it never interferes with a real, long conversation.
- Both `ai_enabled` and the global `auto_reply_enabled` setting are re-checked at job execution time, not just at dispatch time, since state can change during the debounce window.
- **Gemini free tier is capped at 20 requests/day** for `gemini-2.5-flash` (hit for real during 2026-07-16 testing — logged `RESOURCE_EXHAUSTED` errors). The default `AUTO_REPLY_DAILY_LIMIT=25` is actually *higher* than what the free API key allows, so in practice the Gemini quota will bite before our own ceiling does. `MemoryEngine::suggestAutoReply` fails soft (`needs_reply: false` on any exception), so a quota error just means silence, not a crash — but be aware of this if auto-reply seems to "stop working" partway through a day. Production is now using a separate key + `gemini-2.5-flash-lite` (own quota pool, different model) to work around this.
- **Not every Gemini model reliably follows a "return only JSON" prompt instruction.** Confirmed 2026-07-17 in production: `gemini-2.5-flash-lite` responded to the auto-reply prompt with plain conversational text (`"Tentu, ada yang bisa dibantu?"`) instead of the requested `{"needs_reply":...}` JSON — logged as a parse failure and correctly failed soft (no crash, no reply sent), but no reply ever went out either. Fixed by using Gemini's native structured-output support instead of relying on prompt wording alone: `GeminiProvider::chat()` (and `OpenAIProvider::chat()`, analogously) now takes a `bool $jsonMode = false` parameter that sets `generationConfig.responseMimeType = 'application/json'` (Gemini) / `response_format: {type: 'json_object'}` (OpenAI) on the request. `extractMemories()`, `analyzeStyle()`, and `generateAutoReply()` all pass `jsonMode: true`; `generateReply()` (plain-text suggestion, not JSON) intentionally does not. This is a stronger guarantee than prompt instructions alone and should be used for any future method that needs structured output from either provider.

## Production deployment: no persistent worker, no PHP-FPM
Target hosting is Rumahweb shared hosting (cPanel). Confirmed directly with their support (2026-07-16/17):
- **`fastcgi_finish_request()` is unavailable** on this hosting tier — confirmed explicitly by their admin team ("bisa menggunakan layanan VPS / VPS cPanel" if that's needed). This rules out the PHP-FPM-based debounce trick permanently for this host, not just "untested" — don't re-propose it here.
- **No persistent background processes** — `php artisan queue:work` as a long-running daemon is not supported. Only cron is available.
- **`queue:work` itself is not policy-restricted**, but the account has a hard ceiling of **40 concurrent processes total** (shared across everything on the account — web requests, cron, etc, not just this app; visible in cPanel Statistics as "Number Of Processes"). Rumahweb's own generic safety suggestion was "kasih jeda tiap 15 menit" (space runs out by 15 minutes) — but that's a blanket suggestion for all their customers, not a rule tailored to a low-traffic single-user app like this one.
- **Decided approach** (revised three times — see below): cron `* * * * *` triggers `php artisan schedule:run`, which drains the queue via a scheduled task in `routes/console.php`. Matches the convention already used by other Laravel projects on this same cPanel account (`chatbot.fajarnugroho.info`, `aikitab.fajarnugroho.info`). `--stop-when-empty` (no `--max-time`) exits within a second or two once the queue is drained rather than holding the process open — an earlier plan using `--max-time=55` to approximate a persistent worker was rejected as functionally "pretending to be a daemon," exactly what Rumahweb's process-count caution is about.
- **`proc_open()` is also disabled** (confirmed 2026-07-17 from a real production log: `Symfony\Component\Process\Exception\LogicException: The Process class relies on proc_open, which is not available on your PHP installation`). This broke the first version of the scheduled task, which used `Schedule::command('queue:work --stop-when-empty')` — that method spawns the command as a **separate OS process** via Symfony Process, which needs `proc_open`. Fixed by switching to `Schedule::call(fn () => Artisan::call('queue:work', ['--stop-when-empty' => true]))->name('process-auto-reply-queue')->everyMinute()->withoutOverlapping(5)` in `routes/console.php` — `Schedule::call()` with `Artisan::call()` runs the command **in-process**, no subprocess spawned at all, sidestepping `proc_open` entirely. Note `withoutOverlapping()` on a `CallbackEvent` requires an explicit `->name(...)` first (unlike `Schedule::command()`, which can derive one from the command string) or it throws `LogicException: A scheduled event name is required`. Verified locally: dispatched a real job, confirmed `schedule:run` actually dequeues and processes it (not just "runs without erroring") — the `jobs` table went from 1 row to 0.
- This is the third disabled-function discovery on this host (`symlink`/`exec`, `fastcgi_finish_request`, now `proc_open`) — all consistent with one security-hardening policy. **Assume any PHP function that spawns an external process or shells out is unavailable here**; don't reach for `exec`, `shell_exec`, `system`, `passthru`, `proc_open`, or anything built on Symfony Process (which includes `Schedule::command()`/`Schedule::exec()`, not just direct `Process` usage) without testing on this host first.
- **Latency trade-off of the debounce-via-cron design**: replies are no longer near-instant after the 12s debounce — expect roughly 20-70 seconds in practice (12s debounce + up to ~60s waiting for the next cron tick to pick the job up), not a big deal for a WhatsApp auto-reply feature, but worth knowing if it ever feels "slow."
- **`symlink()` and `exec()` are both disabled** (confirmed 2026-07-17: `php artisan storage:link` failed with `Call to undefined function Illuminate\Filesystem\exec()` — Laravel's symlink helper tries `symlink()` first, falls back to shelling out to `ln -s` via `exec()` if `symlink` isn't available, and neither exists here). Standard shared-hosting security hardening, consistent with the missing PHP-FPM/`fastcgi_finish_request`. Consequence: `storage:link` can never work on this host — if a feature ever needs to serve files from `storage/app/public` publicly, it'll need a Laravel route that streams the file directly instead of relying on the symlink. Not an issue yet — nothing in the app serves files through storage (the `avatar` column on `contacts` is just a string, no upload endpoint exists).

### Production target
- Domain: `wassis.fajarnugroho.info`, document root `public_html/wassis.fajarnugroho.info/public` → Laravel project root is one level up: `/home/fajh6696/public_html/wassis.fajarnugroho.info` (cPanel username inferred as `fajh6696` from the DB username/name prefix — verify if anything cPanel-path-related doesn't line up).
- Repo: `git@github.com:nugrohofajar88/wassis.git`, pushed 2026-07-17 (initial commit `3bf320f`, branch `main`). Deploy-by-`git pull` on the server is the intended flow — `git pull` alone does **not** run `composer install`, apply `.env`, run migrations, or touch the cron job, those are still separate manual steps after each pull that changes dependencies/schema.
- DB name `fajh6696_wassis`. **Credentials live only in the server's own `.env`, never in this repo or in this file.**
- Webhook URL once live: `https://wassis.fajarnugroho.info/api/webhooks/fonnte/{secret}` — production uses its own `FONNTE_WEBHOOK_SECRET`, deliberately different from the local dev one, generated 2026-07-17.
- Production `.env` deliberately differs from local dev in: `APP_ENV=production`, `APP_DEBUG=false` (local dev has this `true` — must not carry over, leaks stack traces/`.env` contents publicly on error), a freshly generated `APP_KEY` (never reuse the dev key), `DB_HOST=localhost` (vs. `127.0.0.1` locally — standard for cPanel same-server MySQL), `LOG_LEVEL=error` (vs. `debug`).

### Crontab entry
```
* * * * * /usr/local/bin/ea-php84 /home/fajh6696/public_html/wassis.fajarnugroho.info/artisan schedule:run >> /dev/null 2>&1
```
Matches the exact format already used by the account's other Laravel projects (`chatbot.fajarnugroho.info`, `aikitab.fajarnugroho.info`) — `/usr/local/bin/ea-php84` is cPanel's standard convenience symlink for the PHP 8.4 CLI binary (equivalent to the fuller `/opt/cpanel/ea-php84/root/usr/bin/php` path found via `which php84`; both resolve to the same binary, this project just follows the account's existing convention). **Don't use the `php84` shell alias in the crontab** — `which php84` shows it's `alias php84='/opt/cpanel/ea-php84/root/usr/bin/php'`, and cron runs a non-interactive shell that doesn't source `.bashrc`/`.bash_profile`, so shell aliases aren't available there; a crontab line using bare `php84` would fail with "command not found". Always use an absolute path (either of the two above) in cron entries, never an interactive-shell alias.

`schedule:run` itself does the actual `queue:work --stop-when-empty` call — see the "Decided approach" bullet above and `routes/console.php`. No separate log file for the queue processing (matches the account's other projects piping to `/dev/null`) — if auto-reply seems to silently stop working, check `storage/logs/laravel.log` instead, since job failures inside `ProcessAutoReply` log there via `Log::warning`/`Log::error`, not to cron's own output.

## Companion mobile app (jarwo_app, Expo/React Native)
A mobile client (dashboard/management, not a WhatsApp-notification reader) is being built as a separate project alongside this repo — plan approved 2026-07-17, see `C:\Users\nugro.FAJAR-NB\.claude\plans\distributed-popping-whale.md` for the full design.
- **Stack pivot from Flutter to Expo/React Native (2026-07-17)**: Flutter was the original plan, but the Flutter SDK zip download repeatedly failed on this machine's network (`Invoke-WebRequest` connection reset partway through a ~1GB download; a BITS-transfer retry failed outright). The user already has a working Expo/React Native project, `c:\xampp\htdocs\pioneer-cnc-mobile` (Expo SDK 54, RN 0.81, TypeScript, axios, expo-secure-store, EAS) — switched to that stack to reuse a known-working setup instead of fighting the download. The Android SDK command-line tools installed to `C:\Android` during the aborted Flutter attempt were left in place (`ANDROID_HOME` set) since they're harmless and could be reused later for a custom `expo run:android`/EAS local build, but aren't required for the Expo-Go-based v1 workflow.
- **Scope for v1**: Login, Contacts (list/detail/`ai_enabled`+`is_active` toggles), per-contact chat history + send + AI suggest-reply, WhatsApp export import, style profile view (read-only, via the endpoint above), global `auto_reply_enabled` setting toggle. Events/calendar, Memories browsing, and FCM push are deferred to v2.
- **Stack**: Expo (managed workflow, TypeScript template), `axios` (HTTP, `setAuthToken()` module function rather than an interceptor), `expo-secure-store` (Sanctum token), `expo-document-picker` (`.txt` only, for the import feature). No navigation library and no external state library — screen switching lives in `App.tsx` via `useState`, matching `pioneer-cnc-mobile`'s pattern exactly (see that project's `App.tsx`/`src/services/api.ts`/`src/services/storage.ts` for the concrete pattern to mirror).
- **Dev connectivity**: backend runs locally via `php artisan serve`; the app runs via **Expo Go** (installed from Play Store on the phone, not a locally-built native binary) — phone and dev machine on the same Wi-Fi, `API_BASE_URL` in `src/config.ts` points at the dev machine's LAN IP (no `adb reverse`/USB pairing needed — that was Flutter/native-specific and no longer applies). For the import feature's queued analysis job to actually run locally, either run `php artisan queue:work` in a spare terminal or set `QUEUE_CONNECTION=sync` in `.env` temporarily — there's no local cron equivalent to the production `schedule:run` setup.
- This app has its own project folder/repo (not nested inside this Laravel repo) — treat it as a separate codebase that happens to consume this API; update this section (not add mobile-specific bug patterns into the Laravel-focused sections above) as the client evolves.
- **Windows Firewall blocks the phone from reaching the dev servers by default** (found 2026-07-17, error was Expo Go's generic `java.io.IOException: Failed to download remote update`): the dev machine's Wi-Fi adapter is on the **Public** firewall profile, which blocks unsolicited inbound connections — including from the owner's own phone on the same Wi-Fi trying to reach Metro (port 8081) or `php artisan serve` (port 8000). `curl` from the dev machine itself (even using the LAN IP as the request target) does **not** surface this, since loopback/same-host traffic isn't subject to the same inbound filtering — only a real cross-device request exposes it. Fix requires an elevated (Run as Administrator) PowerShell, since rule creation needs admin rights:
  ```
  New-NetFirewallRule -DisplayName "Expo Metro Dev Server (8081)" -Direction Inbound -Protocol TCP -LocalPort 8081 -Action Allow -Profile Any
  New-NetFirewallRule -DisplayName "Laravel Dev Server (8000)" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow -Profile Any
  ```
- **`create-expo-app@latest` scaffolded Expo SDK 57, but the owner's installed Expo Go (Play Store, no update available) only supports SDK 54** — opening the project threw "incompatible SDK version." Expo Go generally only supports the single latest SDK that's finished rolling out to app stores, and there's a real lag between an SDK release and Expo Go's store build catching up (confirmed via web search: SDK 57 had just gone live with its Expo Go build still "on its way" to Play Store as of 2026-07-17) — don't assume `@latest` is safe to scaffold with when a physical device via Expo Go is the test target. **Downgraded to SDK 54** to match: `npx expo install expo@^54.0.0 --fix` (auto-corrects all `expo-*` packages, `react`, `react-native`, `@types/react`, `typescript` to their SDK-54-compatible versions) — this also happens to now match `pioneer-cnc-mobile`'s versions exactly (Expo ~54.0.33, RN 0.81.5, React 19.1.0), so the two sibling projects stay aligned. If Expo Go ever gets updated past SDK 54 and a newer SDK is wanted, re-run the same `expo install expo@^<target>.0.0 --fix` pattern rather than hand-editing individual package versions.
- **Safe area insets** (found 2026-07-18: top headers and the bottom tab bar visually collided with the status bar and Android's gesture nav bar): added `react-native-safe-area-context`. Fixed centrally rather than per-screen — `App.tsx`'s root is wrapped in `SafeAreaProvider`, and each top-level render branch (`booting`, `!session`, main) uses `SafeAreaView` (`edges={['top']}` for the main branch, since `BottomTabs` handles the bottom edge itself via `useSafeAreaInsets().bottom`) instead of a plain `View`. Because every screen renders as a child inside that single `SafeAreaView`, none of the individual screen files needed their own safe-area handling — don't add per-screen `SafeAreaView`/inset logic, it would double up the padding. If a new top-level screen is ever added outside `App.tsx`'s existing render tree (e.g. a true full-screen modal that escapes the normal component hierarchy), it will need its own `useSafeAreaInsets()`/`SafeAreaView` handling since it won't inherit this fix automatically.
- **Rebrand + pointed at production (2026-07-18)**: `app.json`'s `name` is now "Wassis" (was "jarwo_app", the internal folder/codename) and `slug` is now `"wassis"` (safe to change — no EAS project/`eas.json` exists yet for this app, so nothing was linked to the old slug). App icon regenerated (`assets/{icon,android-icon-foreground,android-icon-background,android-icon-monochrome,favicon}.png`, dark `#111827` background + orange `#ea580c` "W" glyph, matching the app's existing color scheme) via a one-off `sharp`-based SVG-to-PNG script (not part of the repo, was a scratch script). `adaptiveIcon.backgroundColor` also changed from Expo's default light blue to `#111827` to match. **Important**: the new name/icon only becomes the actual phone home-screen icon once a real standalone/dev-client build exists (`expo run:android` or an EAS build) — while iterating via the plain Expo Go sandbox app, the home screen icon stays Expo Go's own icon; the custom name/icon only show up inside Expo Go's UI (title bar, "recently opened" list) and in a future real build. `src/config.ts`'s `API_BASE_URL` now points at production (`https://wassis.fajarnugroho.info/api`) by default, with the old local-dev LAN URL kept commented out for switching back during further local testing — **this means the app now operates on real production data** (real contacts/messages/settings), not a local sandbox.
- **v1 screens built 2026-07-17** (`c:\xampp\htdocs\jarwo_app`, `npx tsc --noEmit` passes clean): `App.tsx` (session bootstrap, bottom tabs, lightweight in-memory screen stack — no navigation library), `src/screens/{LoginScreen,ContactsListScreen,ContactDetailScreen,ChatScreen,ImportChatScreen,SettingsScreen}.tsx`, `src/components/BottomTabs.tsx`. `ChatScreen` fetches the *last* paginator page on load (not page 1) since `MessageController::index` sorts ascending and paginates from the oldest message — page 1 would show the oldest 50 messages first, not the most recent ones a chat UI needs; "load older" then walks `page - 1` backwards from there. `SettingsScreen` also exposes a `default_persona` text field (save button, only enabled when dirty) — see the persona/re-analysis note above.
- **Not yet done**: run `npx expo start` in `jarwo_app` and test end-to-end via Expo Go on the physical phone against the local Laravel server (`php artisan serve`, default port 8000 — `src/config.ts`'s `API_BASE_URL` must match both the port and the dev machine's current LAN IP, which can change between sessions/networks).

## Working agreement
- Whenever code changes, update the relevant file(s) in `AI_Personal_Relationship_Assistant_Docs/` **and** this file in the same turn — don't wait to be asked.
- Don't run real outbound WhatsApp sends (`POST /contacts/{id}/messages`) during testing/smoke-checks without the user's go-ahead — it hits the real Fonnte API with the real token.
